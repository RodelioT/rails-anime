<p id="notice"><%= notice %></p>

<h1>
  <strong>Name:</strong>
  <%= @producer.name %>
</h1>

<p>List of Anime:</p>
<ul>
  <% @producer.animes.each do |anime| %>
    <li><%= anime.name %></li>
  <% end %>
</ul>

<p>List of Employees:</p>
<ul>
  <% @producer.employees.each do |employee| %>
    <li><%= employee.name %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_producer_path(@producer) %> |
<%= link_to 'Back', producers_path %>
